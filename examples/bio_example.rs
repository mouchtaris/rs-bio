fn main() {}

// Examples found in modules:
mod example {
    mod composer_stream;
    mod concat_input;
    mod each_consecutive;
    #[path = "type.IO.rs"]
    mod type_io;
    #[path = "type.Source.rs"]
    mod type_source;
}
